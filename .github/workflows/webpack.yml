name: build front

on:
  push:
    branches: [master]
  pull_request:
    branches: [master]

jobs:
  build fron:
    runs-on: ubuntu-latest

    steps:
      - name: Build
        run: cd web
      - name: aaa
        run: npm run build
